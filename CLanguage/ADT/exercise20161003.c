//// 注释可以这样写，用两个斜杠代表注释，程序运行时会自动忽略
//// 这是C语言入门的第一个程序，在屏幕上打印 hello world
//#include <stdio.h> 
//// stdio.h是一个头文件，头文件用.h表示，现在你不需要知道他是什么
//// 只需要知道每个程序都需要这一行就行了
//int main() // main主函数，是每个程序必须的函数，也是程序的入口函数
//{
//	printf("hello world"); // printf 是包含在stdio.h里的一个函数
//	return 0; // 函数可以有返回值，这里的返回值目前没有什么意义，写上就好了不用管是什么。
//}
//// 所以整个程序关键是printf语句


#include <stdio.h>
int main()
{
	printf("hello world");
	return 0;
}